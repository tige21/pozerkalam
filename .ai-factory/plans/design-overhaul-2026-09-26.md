# План: перестройка UI по style-lock и аудиту (2026-09-26)

Режим: /web-design-engineer overhaul · один проход по CSS и разметке `index.html`. Игровая логика в `<script>` не трогается: правятся только строки оверлеев, подписи, форматирование чисел для показа и DOM-обвязка кнопок.

Чек-лист пунктов: `.tastemaker/design-audit.md`. Система: `.tastemaker/style-lock.md`.

## Фазы
1. Доступность и контраст (#192 #193 #194) — кнопки вместо div, reduced-motion, opacity селектора.
2. Телефон 568×320 (#195) — смещения ≡/⟲, mt-check + размер.
3. Тексты и форматы (#196) — кнопки-статусы, «Заново», запятая, эмодзи, секция строгих уровней.
4. CSS-система — шрифт формы, размеры подписей, hover-gating, радиусы, `.opt`-ячейки, `.lvcard.on`.
5. Проверка — gherkin-run, gherkin-check, unit-check, mt-check, crash-check, скриншоты 4 вьюпортов, контраст.

## Готово когда
- все гейты зелёные, `tools/mirror-script.sh --check` чист;
- скриншоты 1440/844/667/568 без наложений контролов;
- чек-лист аудита отмечен, задачи #192–#196 закрыты.

# Implementation Plan: #154 — показы городских уровней стоят на месте

Branch: main (create_branches: false)
Created: 2026-09-23
Доска: #154 (P1) — Vikunja недоступна при VPN, синхронизировать позже

## Settings
- Testing: yes — регрессия DEMOS (0 таймаутов — гейт снова обязан быть чистым), живой показ 27–31 (машина
  доезжает), `demo-vio` 26–31 (показ не нарушает), `level-audit` (0 касаний, карточки), `traffic-check`,
  gherkin/unit/mt; `@domain`-сценарий или утверждение на правило «показ тормозит перед машиной, а не перед бордюром»
- Logging: `[FIX:demo]` в предупреждении, если показ простоял дольше 3 с из-за машины впереди
- Docs: yes — CLAUDE.md, абзац про поток и показы

## Roadmap Linkage
Milestone: "none"
Rationale: регрессия с c1dd21e, ломает показ на проде.

## Research Context (codegraph + трассировка сегментов)
- `demoStep` (index.html:8897), добавлено в c1dd21e: `if(level.def.traffic && car.gear>=0 && lastClear.front<2.2) тормоз`
  — «показ не таранит поток».
- `lastClear.front` — датчик `clearances()`: пять прямых лучей + диагональ от каждого переднего угла, сложенная
  в «спереди» и в боковое показание (CLAUDE.md, Collision & clearances). В правой полосе городской улицы
  диагональ правого переднего угла упирается в бордюр тротуара: `front = 1,12 м` без единой машины впереди.
- Трассировка `computeIdealPath` уровня 27: все сегменты — `vmax 0`, тормоз на каждом кадре; то же на 29, 30.
- Живой показ: 8 с на уровнях 27, 29, 30 — машина не сдвинулась (0 м), на 25 (без потока) — 10,5 м.
  Значит, на проде кнопка «демонстрация» на уровнях 27–31 не работает, эталонной линии нет, а `demo-vio`
  был «чистым», потому что показ никуда не ехал.

## Tasks
- [x] Task 1: `demoTrafficGap()` — зазор до ближайшей МАШИНЫ (`level.actors`) пятью прямыми лучами из передней
  кромки (`castMin(..., level.actors)`), без диагоналей и без статичных препятствий; правило торможения — по нему.
- [x] Task 2: проверка — утверждение в `tools/units.js`: машина на улице у бордюра, впереди пусто → показ не
  тормозит; машина потока в 1,5 м впереди в полосе → тормозит.
- [x] Task 3: гейты: регрессия DEMOS — 0 таймаутов; живой показ 27–31 доходит до конца; `demo-vio` 25–31 чисто;
  `level-audit` 0 касаний; `traffic-check`; gherkin/unit/mt; мутации `trafficGap`-соседей не задеты.
- [x] Task 4: CLAUDE.md — правило: датчик «спереди» включает диагонали и бордюры, решения «есть ли кто впереди»
  на нём не принимать.

## Решения по ходу
- Task 2 сделан сценариями, а не утверждением: при починке показа всплыл второй дефект — детектор непропуска
  штрафовал за машину, едущую следом в своей полосе (показы 30/31 и любой игрок, медленно подъезжающий к линии
  «уступи» с машиной за спиной). Сценарии `@traffic-yield-follower` (красный до правки) и `@traffic-yield-cut-in`
  (настоящий непропуск по-прежнему штрафуется) в `specs/features/traffic/nepropusk.feature`, шаги в domain.js.
- Показы 30/31 останавливаются до зоны уступания (`uGte 56`, `vGte −17.5`): со старыми концами въезжали в зону на ходу.
- Итог: регрессия DEMOS — 0 таймаутов (впервые с c1dd21e); живые показы 27/29/30 едут; demo-vio 25–31 без
  нарушений, 30 — 73,2 с, 31 — 47,5 с; level-audit 0 касаний; detectorCheck 16/0, examCheck 5/0, routeCheck 6/6;
  gherkin 41/0 и 22/22; unit 126/0; mt 40/0; traffic 8; light 3; мутации violationsTick 32,8 → 34,4 %.

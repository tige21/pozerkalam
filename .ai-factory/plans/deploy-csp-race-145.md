# Implementation Plan: #145 — смоук зеркала ложно падает на CSP

Branch: main (create_branches: false)
Created: 2026-09-23
Доска: #145 — Vikunja недоступна при VPN, синхронизировать позже

## Settings
- Testing: yes — функция `csp_wait`, вырезанная из скрипта: живые прод/зеркало; заглушка `curl` (2 раза старый
  заголовок → новый; вечный старый → провал); `bash -n`
- Logging: в выводе смоука — «с N-й попытки», если заголовок сошёлся не сразу
- Docs: yes — CLAUDE.md, раздел про зеркало

## Research Context
- 23.09 на обоих деплоях смоук зеркала: «2 инлайн-скрипта вне заголовка»; ручная перепроверка через секунды —
  0 непокрытых на обоих боксах.
- `deploy-pozerkalam.sh`: rsync файлов → копия `pozerkalam-headers.conf` → `systemctl reload nginx` → сразу curl →
  `csp_check`. Reload асинхронный: старые рабочие дослуживают соединения со старым заголовком, а страница уже новая.
  У прода та же гонка, её маскировал `sleep 1` перед смоуком.

## Tasks
- [x] Task 1: `csp_check` с тихим режимом; `csp_wait ip path where` — до 10 попыток с паузой 1,5 с, каждый раз
  свежий curl через `--resolve`; не сошлось за 15 с — провал с печатью непокрытых хэшей.
- [x] Task 2: прод (`/play/`, `/`) и зеркало (`/play/`) — через `csp_wait`.
- [x] Task 3: проверка функции: живые боксы, заглушка «сошлось с 3-й», заглушка «не сходится» — провал.
- [x] Task 4: CLAUDE.md.

# Ralph-цикл: правки заказчика 23.09.2026

Запущен 2026-09-23 по просьбе владельца: по каждой правке — поиск по кодовой базе через codegraph,
план `/aif-plan full`, реализация `/aif-implement`, ревью `/aif-verify`; цикл крутится, пока не закрыты все правки.
Промт цикла: «Прочитай .ai-factory/RALPH-CUSTOMER-2026-09-23.md и выполни ровно одну итерацию по его правилам».
Обещание завершения — `CUSTOMER_DONE` в promise-теге, ТОЛЬКО когда у всех пяти задач в журнале `done`
(или `ждёт владельца` с причиной). В обычных отчётах тег не писать.

## Задачи (порядок = порядок работы)

| # | Доска | Правка заказчика |
|---|---|---|
| 1 | #149 | Итоги урока: модалка показывает только итоги урока и комментарий, кнопки «Выбрать уровень», «Повторить», «Следующий уровень»; список уровней убрать; рядом с «Следующий уровень» — название следующего уровня. |
| 2 | #150 (ждёт #149) | Убрать таймер из интерфейса занятия, итоговое время показывать только в итогах урока. |
| 3 | #151 | Подсказка управления во время урока — блок в левой части окна, максимум одна подсказка на строку; сейчас неудобно читать и на узких экранах блок перекрывает интерфейс. |
| 4 | #152 | Индикатор текущего положения руля: на втором обороте цвет линии отличается от цвета на первом (текст пришёл обрезанным; трактовка — сделать так, чтобы второй оборот был виден другим цветом; если в коде это уже так — выяснить, о каком индикаторе речь, и записать вопрос владельцу). |
| 5 | #153 | Баг: сцепление — значение в процентах клипует при 100 %. |

## Правила итерации

0. Прочитать журнал (внизу). Взять первую задачу таблицы без `done`/`ждёт владельца` в журнале.
   На доске — в Doing (`POST /projects/3/views/12/buckets/8/tasks {"task_id":N}`); в журнал `#N — в работе (итерация K)`.
1. **Поиск — только codegraph** (`codegraph_explore` / `codegraph_search` / `codegraph_callers` / `codegraph_impact`
   по зеркалу `codegraph-src/index.js`, строка N == `index.html:N`). Serena к inline-скрипту в `.html` неприменима —
   это записать в отчёт, а не обходить grep'ом. Grep/Read — только чтобы открыть уже найденную codegraph строку.
2. `/aif-plan full <суть задачи>` → план в `.ai-factory/plans/<slug>.md` (ветки не создаются: `create_branches: false`).
3. `/aif-implement` по плану. Правится только `index.html` (зеркало пересобирает pre-commit).
   Пользовательский текст — голос `.ai-factory/references/pozerkalam-voice.md` + сканер `ru-copy-deslop`.
4. Проверки после реализации (все обязательны при правке `index.html`):
   - headless-регрессия: `loadLevel` по всем ключам `DEMOS` с перехватом `console.warn` — 0 таймаутов;
   - `node tools/gherkin-check.mjs`, `PW_DIR=/tmp/pw node tools/gherkin-run.mjs`, `PW_DIR=/tmp/pw node tools/unit-check.mjs`;
   - для HUD/тача — `tools/mt-check.mjs` (перекрытия кнопок на телефонах, сцепление);
   - глазами — скриншот в Playwright (десктоп 1440×900 и телефон 667×375) через `python3 -m http.server 8471`, с `?nocache=<ts>`.
5. `/aif-verify` по плану; найденные дефекты починить в той же итерации, повторить verify.
6. `/aif-commit` (без упоминаний ИИ в сообщении). Закрыть задачу на доске полным payload (`done:true`).
   В журнал: `#N — done: <что было не так / что сделано / как проверено>, <хеш>`.
7. Одна задача за итерацию. Три итерации без прогресса по задаче → `ждёт владельца` с причиной и комментарием на доске.
   Новая работа, найденная по ходу, — задача на доске в том же ходе + строка в журнале.
8. Деплой на прод — не делать (владелец решает отдельно).

## Итоговый отчёт (после последней задачи, перед тегом)

По каждой правке: что было в коде (с `index.html:строка`), что изменено, чем проверено, хеш коммита.

## Журнал
- итерация 1 · #149 — в работе (итерация 1)
- итерация 1 · #154 — новая (P1): демо уровней 27–31 уходят в таймаут сегментов с коммита c1dd21e (найдено гейтом регрессии; вне правок заказчика, в этот цикл не входит)
- итерация 1 · #149 — done: было — `winHTML()` (index.html:8092, обе ветки: уровень и дрилл) дописывал `lvlListHTML()` — список всех 32 уровней с кнопкой выбора — и ряд «Следующий / Повторить / ✉». Сделано — общий хвост `winActsHTML()`: итоги + комментарий (совет, «Запомни для реальной дороги») + «Выбрать уровень» / «Повторить (R)» / «Следующий уровень (N)» с именем следующего уровня под кнопкой (после последнего — уровень 1, ★ для своей площадки, имя через `esc`); «✉ Что-то не так» — тихая ссылка под рядом (канал отзывов с экрана победы сохранён — вопрос владельцу, убрать ли совсем); на телефоне ряд из трёх кнопок (столбиком главная уходила за край). Проверено — скриншоты 1440×900 и 667×375 (уровень, дрилл, последний), `doAct('resume')` пересобирает тот же экран, gherkin-check 34/0, gherkin-run 20/20, unit-check 117/0; регрессия демо — 28 таймаутов на 27–31, те же на HEAD (регрессия с c1dd21e → #154). Коммит 31d61d0.
- итерация 2 · #150 — в работе (итерация 2)
- итерация 2 · #150 — done: было — `#timeVal` в `#topright` (index.html:445) крупным шрифтом, `updateHUD` (index.html:7536) писал `game.t` каждый кадр. Сделано — элемент и запись удалены, крупной строкой панели стал «касаний: N» (+ «уровень X / 32»); итоговое время уже есть в итогах урока (`winHTML` «Время: X с») и в протоколе экзамена, в рекорд пишется как раньше; у дриллов время в итогах не показывается намеренно (мерило — см). Потребителей `timeVal` в tools/landing/скриптах сборки нет. Проверено — скриншоты HUD 1440×900 и 667×375, gherkin-check 34/0, gherkin-run 20/20, unit-check 117/0, mt-check 39/0 (перекрытий тач-полосы нет), регрессия демо — только известные таймауты #154. Коммит 77ca402.
- итерация 3 · #151 — в работе (итерация 3)
- итерация 3 · #151 — done: было — `syncHintLine()` (index.html:5382) писал в `#hint` одну строку из 15 команд через « · »; CSS `#hint` (index.html:223) `left:14px; bottom:200px` без ширины — строка шла через весь экран поверх миникарты; левый край занят карточкой уровня, левым зеркалом (`mirrorRects`) и панелью руля (`drawSteerPanel` на H−186). Сделано — столбик «клавиша | действие», одна команда на строку, по важности (`HINT_AT`/`HINT_MT` + `HINT_ALL`); `layoutHint()` вписывает его в полосу между низом карточки/зеркала и верхом панели руля, лишние строки прячет, «H — справка и все клавиши» всегда последняя, меньше двух строк — блок скрыт; пересчёт раз в 15 кадров. Проверено — замер пересечений с карточкой, зеркалом, панелью руля и нижней панелью на 1440×900 / 1280×720 / 1024×640 / 1920×1080 / 1366×768 × зеркала вкл/выкл × АКПП/МКПП — 0 из 20; колонка клавиш не обрезает подписи; скриншоты; gherkin 34/0 и 20/20, unit 117/0, mt-check 39/0, mirror-check 12/0; регрессия — только #154. Коммит eddedf3.
- итерация 4 · #152 — в работе (итерация 4)

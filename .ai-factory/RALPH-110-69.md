# Ralph-цикл: #110 (линзы светофора) и #69 (телефон: JS)

Запущен 2026-09-23 по просьбе владельца: «бери 110 69, сделай их полностью через ralph-loop + aif».
Промт цикла: «Прочитай .ai-factory/RALPH-110-69.md и выполни ровно одну итерацию по его правилам».
Обещание завершения — `TASKS_110_69_DONE` в promise-теге, ТОЛЬКО когда у обеих задач в журнале `done`
(критерии ниже выполнены и подтверждены замером) или `ждёт владельца` с причиной. В обычных отчётах тег не писать.
Все ответы владельцу — по-русски.

## Задачи

### #110 (P1) — из салона линзы светофора местами закрывает кабина (остаток)
Источник: коммит a7d237b («остаток вне этой правки: из салона на 8/5/4.5/4 м часть линз закрывает кабина —
пиксель идентичен до и после, bias на него не влияет»).
Замер 23.09 на HEAD 26c3015 (`PW_DIR=/tmp/pw node tools/light-check.mjs`):
- **chase (инвариант) красный**: 10 закрытых линз на 12/10/8/7 м, пиксель 29,79,159 — синий, как табличка
  названий улиц (`guideSign`, появилась после a7d237b). Регрессия: гейт light-check сейчас падает на main.
- салон: 7 закрытых на 8/7/5/4.5/4 м — серые 99–105 (обшивка?) и тёмные 44–51.
**Критерий done:** chase — 0 закрытых на всех дистанциях; салон — каждая линза, которая геометрически видна
через лобовое стекло, горит; линзы, честно ушедшие за верхнюю кромку лобового (как в жизни), отделены в
инструменте проверкой «центр линзы внутри проёма стекла», а не на глаз; на уровне 27 из салона на стоп-линии
игрок видит сигнал (иначе уровень учит стоять на красный вслепую). Инструмент выходит с кодом 0.

### #69 (P1) — телефон: уровни качества снижают работу JS
Источник: 1c1a6de (06.09): лестница QUALITY режет материалы/детали/дальность/руль; CPU×4: q0 15 мс → q5 9,6 мс.
Было «ждёт проверки на реальном телефоне». С тех пор 82 коммита, в т.ч. плотный поток (до 12–13 машин,
лофт 13 станций ближе TRAF_LOD 45 м) — замер устарел.
**Критерий done:** `MOBILE=1 CPU=4` (`tools/perf-bench.mjs`) на площадке (ур. 1), салоне, городе с
плотным потоком (ур. 30) и экзамене (ур. 32): регулятор выходит на уровень, где p95 кадра ≤ 33 мс (≥30 fps)
и JS укладывается в бюджет; найденные горячие места в JS (профиль, счётчик граней) устранены, а не
спрятаны снижением качества; десктоп GPU не деградирует (q0, те же fps, OLD=26c3015). Итоговые цифры —
в CLAUDE.md, раздел Adaptive performance. Реальный телефон — только владелец: если после всего
агентского остаётся лишь это, статус `ждёт владельца` с цифрами.

## Правила итерации

0. Прочитать журнал (внизу). Взять первую задачу (порядок: #110, затем #69) без `done`/`ждёт владельца`.
   Доска: попытаться перенести в Doing (`POST /projects/3/views/12/buckets/8/tasks`); Vikunja бывает
   недоступна при включённом VPN — тогда записать в журнал «доска не сверялась» и синхронизировать позже.
1. **Поиск — codegraph** (`codegraph_explore`/`search`/`callers`/`impact` по `codegraph-src/index.js`,
   строка N == `index.html:N`); CSS/HTML codegraph не индексирует — открывать по номеру строки.
2. `/aif-plan full <суть>` → `.ai-factory/plans/<slug>.md` (ветки не создаются).
3. `/aif-implement`. Правится только `index.html` и инструменты в `tools/`.
4. Гейты после правки `index.html`: регрессия DEMOS (известные таймауты #154 — не новые), `gherkin-check`,
   `gherkin-run`, `unit-check`, `mt-check`; для салона и геометрии — `tools/cockpit-shots.mjs`
   (sortAudit 0 в салоне, skyGap в пределах); для светофора — `tools/light-check.mjs`; для перфа —
   `tools/perf-bench.mjs` до/после. Новая проверка в инструменте — с `@ui`-сценарием и кодом требования.
5. `/aif-verify`; дефекты чинить в той же итерации.
6. `/aif-commit` (без упоминаний ИИ). Задачу на доске закрыть полным payload, если доска доступна.
   В журнал: `#N — done: <было / сделано / как проверено>, <хеш>`.
7. Одна задача за итерацию; большая задача может занять несколько итераций — в журнал писать прогресс.
   Три итерации без прогресса → `ждёт владельца` с причиной. Новая работа по ходу — задача на доске + строка.
8. Деплой — не делать без просьбы владельца.

## Журнал
- 2026-09-23 · старт: доска недоступна (VPN), описания задач восстановлены из a7d237b и RALPH-BOARD.md.
- итерация 1 · #110 — done: было — (1) из-за машины на 7–12 м линзы уровня 27 закрывал щит 6.10.1 (`cityWorld` ставил его в hw+1,2 — на линии светофора, 6,9 м перед ним, та же высота), light-check падал на main; (2) из салона сложенный козырёк висел впереди кромки лобового (y 1,31–1,35, z 0,32–0,52) и закрывал полосу 7–16°; (3) стоп-линия на линии светофора — из салона сигнала у стоп-линии не видно. Сделано — щит отодвинут (hw+1,2+GUIDE_HW+0,3), козырёк лежит на потолке за поперечиной (+19,9°), `putLight` ставит светофор + стоп-линию + дублёр за узлом (уровень 27 и экзамен), `lightKerbClear` держит стойки за бордюром (экзаменационная на Садовой заходила на 4 см). light-check: классификация лучом по `WSHIELD`/`CMIR` + строка стоп-линии, 3 проверки с кодами и @ui-сценариями; мутации (старый козырёк, без дублёра) — красные. Проверено — light-check 3/3 OK, cockpit-shots (сорт. салона 0, skyGap 13, кадр 2,81 мс), level-audit 0 касаний, navCheck/routeCheck/detectorCheck зелёные, gherkin 39/0 и 20/20, unit 117/0, mt 40/0, traffic 8 OK, demo-vio L27 чисто; регрессия — только #154. Честные закрытия, оставшиеся по дизайну: салонное зеркало (зелёная на 7–8 м), крыша правее оси (4–5 м). Доска не сверялась (VPN). Коммит de823c6.
- итерация 2–3 · #69 — прогресс: код закоммичен (5392643). Было — на эмуляции телефона (CPU×4, плотный поток) JS 11–15 мс из 16,7; профиль CDP: главный поток занят ~87 %, `clearances` перебирал все препятствия 36 лучами, `rayOBB`/`pathCam` выделяли память на вызов/вершину, `Math.hypot` в горячем пути, грани — десяток скрытых классов. Сделано — `clearNear` (точное надмножество, 600 поз совпали до 1e-6), `raySlab` без памяти, `pathCam`/`pushFace`/`pushQuad`/`pushPoly`/`shadeCol` на sqrt, моноформный объект грани; `perf-bench` получил LEVEL/CAM/TRAFFIC. A/B в одном сеансе: салон ур.1 86→103 fps (JS 10,7→8,3), ур.30 chase 90→117 (9,2→7,4), ур.30 салон 76→96 (10,8→9,0), q0 вместо q5; десктоп 120 fps, JS 3,3→2,9. Мутации: rayOBB 43,5→60 %, castMin 25→33 %, clearances 27,5→45,9 % (утверждения с бордюром 16 см). Гейты: cockpit-shots --sweep (салон 0, toggles 0, skyGap 13, кадр 2,43 мс), регрессия (только #154), gherkin 39/0 и 20/20, unit 126/0, mt 40/0, traffic 8, light 3, hull, mirror — зелёные. Осталось: полный мутационный прогон (идёт), итоговая матрица без фоновой нагрузки, проверка на реальном телефоне — владелец.
